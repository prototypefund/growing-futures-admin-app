import ProductList from '@/components/ProductList.vue'
import loadData from '@/utils/DataLoader.js'

describe('should be true', () => {
  var res = true //loadData([])

  it('should return a value', () => {
    expect(true).toBeTruthy()
  })
})
